# dict

Dictionary CLI to search from Wikipedia

# Install
```bash
$ git clone https://github.com/ksk001100/dict
$ cd dict
$ cargo install --path .
```

# Usage
```bash
$ dict macOS
$ dict macOS --lang en
$ dict macOS -l en
$ dict --help
```